#!/usr/bin/env ruby

require 'rubygems'
require_relative '../lib/cure'

evald_options = Cure::Option.new
env = Cure::Environment.new evald_options.evaluate_input.options
Cure::Curl.new(env.environment).run
