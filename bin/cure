#!/usr/bin/env ruby

require 'rubygems'
require_relative '../lib/cure'

evald_options = Cure::Option.new
evald_options.evaluate_input
env = Cure::Environment.new evald_options.options
curl = Cure::Curl.new env.environment
puts curl
puts curl.run
